version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto8
  build:
    commands:
      - echo Build started on 'date'
    post_build:
      commands:
      - echo Build completed on 'date'
      - nvm packege
artefacts:
  files:
    - target/gs-spring-boot-0.1.0.jar


